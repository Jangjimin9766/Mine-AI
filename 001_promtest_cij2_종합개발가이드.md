# 001_promtest_cij2_종합개발가이드 (Git/서버/GPU/프리뷰)

노트북을 켜서 작업을 시작할 때부터 개발 완료 후 코드를 올릴 때까지 필요한 모든 과정을 순서대로 정리했습니다.

---

## 🏗️ 1. 개발 전 준비 (Git 최신화)

새로운 기능을 만들기 전, 항상 서버의 최신 코드를 내 노트북으로 가져와야 합니다.

```bash
# 1. 서버(GitHub)의 최신 이력 정보만 업데이트
git fetch origin

# 2. 나의 로컬 main 브랜치로 이동
git checkout main

# 3. 로컬 main을 서버의 최신 main과 똑같이 맞춤
git pull origin main

# 4. 이제 최신화된 main에서 새로운 작업 브랜치 생성
git checkout -b feature/[새로운-기능-이름]
```

---

## 🚀 2. 서버 실행하기 (Spring & Python)

두 서버가 모두 켜져 있어야 매거진 생성이 정상 작동합니다.

### A. Spring Boot 서버 (Mine_server)
```bash
# 폴더 이동 후 실행
cd /c/Dev/workspace/project/Mine/Mine_server
./gradlew bootRun
```

### B. Python 서버 (Mine-AI)
```bash
# 폴더 이동 후 가상환경 활성화 및 실행
cd /c/Dev/workspace/project/Mine/Mine-AI
source venv/Scripts/activate  # (Bash 기준)
python -m app.main
```

---

## ☁️ 3. GPU 연동 (이미지 퀄리티 확인 시 선택)

노트북에 GPU가 없을 때 고화질 이미지를 뽑는 방법입니다. (평소엔 생략 가능)
1. **Colab 접속**: [tools/colab_server.py](file:///c:/Dev/workspace/project/Mine/Mine-AI/tools/colab_server.py) 코드를 [Google Colab](https://colab.research.google.com/)에 복사/실행.
2. **주소 입력**: 결과값 `https://xxxx.ngrok-free.dev/generate`를 내 노트북의 **[.env](file:///c:/Dev/workspace/project/Mine/Mine-AI/.env)** 파일 `REMOTE_IMAGE_SERVER_URL`에 저장.
3. **재시작**: Python 서버를 껐다 켭니다.

---

## 🎨 4. 결과 확인 (비주얼 프리뷰)

Swagger 대신 터미널에서 아래 명령어를 실행하여 **실제 앱 카드 디자인 UI**를 확인하세요.
```bash
cd c:\Dev\workspace\project\Mine\Mine-AI
python tools/magazine_preview.py --id [매거진ID] --token "내토큰"
```

---

## ✅ 5. 개발 완료 후 (GitHub 업로드)

작업이 끝나면 서버에 브랜치를 올리고 Pull Request(PR)를 생성합니다.

```bash
# 1. 변경된 파일 로컬 저장소에 추가
git add .

# 2. 커밋 메시지 작성
git commit -m "fix: promtest_cij2 및 고품질 이미지 생성 로직 적용"

# 3. 브랜치를 서버(GitHub)로 업로드 (Push)
git push origin feature/[작업중인-브랜치-이름]
```
> [!NOTE]
> 이후 GitHub 사이트에 접속하여 **Pull Request(PR)**를 생성하고 마무리합니다.

---

## 💻 6. 작업 내용을 노트북으로 옮기는 법 (중요!)

데스크탑에서 작업한 내용을 안전하게 노트북으로 가져가는 순서입니다.

### STEP A. 데스크탑에서 할 일 (Push)
절대 `main` 브랜치에 직접 올리지 마시고, **전용 작업용 브랜치**를 하나 만들어 올리세요.
```bash
# 1. 새 브랜치 생성 (예: feature/promtest-cii2-v5)
git checkout -b feature/promtest-cij2-v5

# 2. 작업 내용 저장 및 업로드
git add .
git commit -m "feat: V5 프롬프트 및 하이브리드 환경 세팅 완료"
git push origin feature/promtest-cij2-v5
```

### STEP B. 노트북에서 할 일 (Pull)
노트북을 켜고 터미널(Bash)에서 아래 명령어를 순서대로 입력하세요.
```bash
# 1. 서버의 최신 정보 업데이트
git fetch origin

# 2. 데스크탑에서 올린 브랜치로 바로 이동하면서 가져오기
git checkout feature/promtest-cij2-v6
```
> [!TIP]
> 이렇게 하면 `main` 브랜치를 건드리지 않고 안전하게 작업 내용만 노트북으로 쏙 가져올 수 있습니다!

---

### 📝 과제 `promtest_cij2` 핵심 기능
- **매거진/섹션 생성** (V5 하이엔드 톤)
- **섹션 본문 수정** (문단 추가, 전체 수정, 문단 삭제)
- **무드보드 생성** (SDXL 연동 강화)
