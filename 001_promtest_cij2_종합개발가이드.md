# 001_promtest_cij2_종합개발가이드 (Git/서버/GPU/프리뷰)

노트북을 켜서 작업을 시작할 때부터 개발 완료 후 코드를 올릴 때까지 필요한 모든 과정을 순서대로 정리했습니다.

---

## 🏗️ 1. 개발 전 준비 (Git 최신화)

새로운 기능을 만들기 전, 항상 서버의 최신 코드를 내 노트북으로 가져와야 합니다.

```bash
# 1. 서버(GitHub)의 최신 이력 정보만 업데이트
git fetch origin

# 2. 나의 로컬 main 브랜치로 이동
git checkout main

# 3. 로컬 main을 서버의 최신 main과 똑같이 맞춤
git pull origin main

# 4. 이제 최신화된 main에서 새로운 작업 브랜치 생성
git checkout -b feature/[새로운-기능-이름]
```

---

## 🚀 2. 서버 실행하기 (Spring & Python)

두 서버와 필수 서비스(Redis 등)가 모두 켜져 있어야 매거진 생성이 정상 작동합니다.

### A. Spring Boot 서버 (Mine_server)
```bash
# 폴더 이동 후 필수 서비스 실행 (예: Redis)
cd /c/Dev/workspace/project/Mine/Mine_server
docker run -d -p 6379:6379 redis

# 서버 실행
./gradlew bootRun
```

### B. Python 서버 (Mine-AI)
```bash
# 폴더 이동 후 가상환경 활성화 및 실행
cd /c/Dev/workspace/project/Mine/Mine-AI
source venv/Scripts/activate  # (Bash 기준)
python -m app.main
```

---

## ☁️ 3. GPU 연동 (이미지 퀄리티 확인 시 필수)

노트북에 GPU가 없을 때 구글 코랩을 빌려 고화질 이미지를 생성하는 방법입니다.

1. **코드 복사**: `Mine-AI/tools/colab_server.py` 파일의 전체 내용을 복사합니다.
2. **코랩 붙여넣기**: [Google Colab]을 열고 새 노트에 코드를 붙여넣습니다.
3. **GPU 설정**: `런타임` -> `런타임 유형 변경` -> **T4 GPU** 선택 후 저장합니다.
4. **서버 실행**: 코랩 왼쪽의 **재생(▶️)** 버튼을 클릭합니다. (약 3~5분 소요)
5. **주소 복사**: 하단에 출력되는 `🔗 COPY THIS URL: https://xxxx.ngrok-free.dev/generate` 주소를 복사합니다.
6. **환경변수 설정**: 내 노트북의 `.env` 파일을 열고 아래 항목에 주소를 붙여넣습니다.
   ```env
   REMOTE_IMAGE_SERVER_URL=복사한_주소_입력
   ```
7. **AI 서버 재시작**: Python 터미널에서 서버를 껐다 켜면 고화질 생성 준비 완료!

---

## 🎨 4. 결과 확인 (비주얼 프리뷰)

Swagger 대신 터미널에서 아래 명령어를 실행하여 **실제 앱 카드 디자인 UI**를 확인하세요.

```bash
cd c:\Dev\workspace\project\Mine\Mine-AI
python tools/magazine_preview.py --id [매거진ID] --token "내토큰"
```

---

## ✅ 5. 개발 완료 후 (GitHub 업로드)

작업이 끝나면 서버에 브랜치를 올리고 Pull Request(PR)를 생성합니다.

```bash
# 1. 변경된 파일 로컬 저장소에 추가
git add .

# 2. 커밋 메시지 작성
git commit -m "fix: promtest_cij2 및 고품질 이미지 생성 로직 적용"

# 3. 브랜치를 서버(GitHub)로 업로드 (Push)
git push origin feature/[작업중인-브랜치-이름]
```

> [!NOTE]
> 이후 GitHub 사이트에 접속하여 **Pull Request(PR)**를 생성하고 마무리합니다.

---

## 💻 6. 작업 내용을 노트북으로 옮기는 법 (Push & Pull)

데스크탑에서 작업한 내용을 안전하게 노트북으로 가져가는 순서입니다.

### STEP A. 데스크탑에서 할 일 (Push)
```bash
# 1. 새 브랜치 생성 및 이동
git checkout -b feature/promtest-cij2-work

# 2. 작업 내용 저장 및 서버 업로드
git add .
git commit -m "feat: V7 프롬프트 고도화 및 환경 세팅 완료"
git push origin feature/promtest-cij2-work
```

### STEP B. 노트북에서 할 일 (Pull)
```bash
# 1. 서버 최신 이력 업데이트
git fetch origin

# 2. 서버에 올라온 브랜치를 그대로 가져오기
git checkout feature/promtest-cij2-work
```

> [!TIP]
> 이렇게 하면 `main`을 오염시키지 않고 깔끔하게 작업 내용을 동기화할 수 있습니다!

---

### 📝 과제 `promtest_cij2` 핵심 기능 리스트
- [x] **매거진/섹션 생성** (V7 멀티-페르소나 톤)
- [x] **섹션 본문 수정** (문단 추가, 전체 수정, 문단 삭제)
- [x] **무드보드 생성** (SDXL 원격 GPU 연동)
- [x] **비주얼 프리뷰** (실제 UI 미러링 구현)
